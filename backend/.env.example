OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PIPELINE_MODEL=gpt-5.6-terra
ELEVENLABS_API_KEY=
# Optional; defaults to ElevenLabs' Rachel voice.
ELEVENLABS_VOICE_ID=
# Voice transcription prefers ElevenLabs Scribe when its key is set, then
# falls back to OPENAI_API_KEY (Whisper) when available.
